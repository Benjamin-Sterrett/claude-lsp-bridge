# Code Review Evidence — PR4: MCP Server Wiring

**Date:** 2026-02-28
**PR:** PR4 — MCP Server Entry Point + Integration Tests (PRJ-243)
**Branch:** feature/prj-243-mcp-server
**Blast Radius:** Medium

## Files Changed
- `src/index.ts` — NEW: MCP server entry point with tool registration, graceful shutdown (192 LOC)
- `tests/integration.test.ts` — NEW: 9 end-to-end MCP protocol tests using Client SDK (162 LOC)
- `src/types.ts` — Added index signatures to ToolSuccess/ToolError for CallToolResult compat
- `package.json` — Added `start` and `dev` scripts

## Reviewers

### 1. Semgrep SAST
- **Result:** 0 findings

### 2. Codex Mid-Implementation Review
- **Verdict:** APPROVE (confidence 0.90)
- **Quote:** "I found no correctness, security, lifecycle, or type-safety regressions in this diff."
- **Findings:** None

### 3. Security Review (haiku)
- **Findings:** 1 CRITICAL + 3 HIGH + 4 MEDIUM + 3 LOW

| Severity | File:Line | Issue | Resolution |
|----------|-----------|-------|------------|
| CRITICAL | index.ts:42 | Command injection via LSP server args passed to spawn | **False positive:** `child_process.spawn()` uses execvp (no shell). Args array is not shell-interpolated. Config is trusted local file with zod validation. |
| HIGH | index.ts:45 | Unvalidated env propagation — process.env forwarded to LSP subprocess | **Accepted risk:** Standard Node.js pattern for child processes. LSP servers need PATH and other env vars to function. Config is local/trusted. |
| HIGH | index.ts:180 | Signal handler DoS — SIGINT/SIGTERM could be sent repeatedly | **False positive:** `shuttingDown` flag prevents re-entry. Process exits after first shutdown. |
| HIGH | types.ts:61-88 | Path traversal via filePathToUri/uriToFilePath | **Already fixed in PR3:** `safeUriToPath()` in format.ts handles malformed URIs. Tool is designed to read arbitrary project files — that's its purpose. |
| MEDIUM | config.ts | Config file injection via LSP_CONFIG env var | **Accepted risk:** Config is read from local filesystem only. Zod validates structure. User controls the env var. |
| MEDIUM | index.ts:33 | Error message disclosure in toolError catch blocks | **Accepted risk:** Error messages go to MCP client (Claude Code) over local stdio. No external exposure. |
| MEDIUM | index.ts:169 | Init options lack additional validation | **Not applicable:** MCP SDK validates all inputs via zod schemas before callbacks execute. |
| MEDIUM | index.ts:45 | Config path disclosure in stderr logs | **Accepted risk:** stderr is local process output. Not exposed to network. |
| LOW | — | Missing input validation documentation | **Noted:** Input validation is handled by zod schemas in registerTool. Self-documenting. |
| LOW | index.ts:176 | Stdio transport availability assumption | **Not applicable:** StdioServerTransport handles stream errors gracefully. |
| LOW | index.ts:186 | Stderr log exposure | **Accepted risk:** stderr is standard for MCP server diagnostics. Local only. |

### Triage Summary

All security findings are either false positives or accepted risks for a **local-only stdio MCP tool**:
- No network exposure (stdio transport only)
- Config is local/trusted with zod validation
- LSP servers are local processes the user explicitly configured
- Error messages stay within local MCP communication

**No code fixes required.** Codex independently confirmed with APPROVE verdict.

## Test Results

- 78/78 tests pass (69 existing + 9 new integration tests)
- TypeScript: clean
- Build: clean
- Semgrep: 0 findings
