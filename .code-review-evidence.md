# Code Review Evidence — PR2: LSP Client + Manager

**Date:** 2026-02-28
**PR:** PR2 — LSP Client + Manager (PRJ-241)
**Branch:** feature/prj-241-lsp-client
**Blast Radius:** Medium

## Reviewers

### 1. Semgrep SAST
- **Result:** 0 findings
- **Scope:** All .ts/.mjs files in src/ and tests/

### 2. Codex Plan Review
- **Verdict:** REQUEST_CHANGES (6 findings, all incorporated into implementation)
- **Findings:**

| Severity | Issue | Resolution |
|----------|-------|------------|
| HIGH | Validate textDocumentSync kind from server | Implemented: parse number vs object form, default to None per spec |
| HIGH | Crash recovery needs state rehydration | Implemented: ensureReady() rehydrates all open documents after restart |
| MEDIUM | Position encoding defaults to UTF-16 if absent | Implemented: fallback to utf-16 when server doesn't specify |
| MEDIUM | Graceful shutdown needs timeout + force-kill | Implemented: SIGTERM with 1s escalation to SIGKILL |
| MEDIUM | Test edge cases missing | Added tests for concurrent init, spawn failure, duplicate extensions |
| LOW | Clear "no configured server" errors | Implemented: descriptive error messages listing configured extensions |

### 3. Codex Mid-Implementation Review
- **Verdict:** REQUEST_CHANGES (6 findings)
- **Findings:**

| Severity | File:Line | Issue | Resolution |
|----------|-----------|-------|------------|
| HIGH | client.ts:147 | textDocumentSync options object: `change` omitted defaults to Full, but LSP spec says None | Fixed: default to `TextDocumentSyncKind.None` when `change` is omitted |
| HIGH | client.ts:56 | `initialize()` not concurrency-safe — parallel calls can spawn duplicate processes | Fixed: added `initPromise` mutex pattern |
| HIGH | client.ts:83 | `process.stdout!`/`stdin!` non-null asserted during connection setup; spawn failures produce null stdio | Fixed: await `spawn` event before creating connection, validate stdio handles |
| MEDIUM | client.ts:65 | stderr piped but never consumed — verbose logging fills buffer and stalls process | Fixed: changed to `stderr: 'inherit'` |
| MEDIUM | manager.ts:15 | Duplicate extension registrations silently overwrite | Fixed: detect collisions and throw descriptive config error |
| MEDIUM | tests:1 | Missing crash recovery and diagnostics caching test coverage | Added: concurrent init test, spawn failure test, duplicate extension test |

### 4. Codex PR Review — Round 1
- **Findings:**

| Severity | File:Line | Issue | Resolution |
|----------|-----------|-------|------------|
| P1 | client.ts:120 | `initializationOptions` from config not forwarded in initialize request | Fixed: added to init params |
| P2 | client.ts:238 | `ensureReady` crash recovery not serialized | Fixed: reuse `initPromise` mutex |
| P2 | client.ts:153 | Initialize timeout timer never cleared | Fixed: `clearTimeout` after handshake |

### 5. Codex PR Review — Round 2
- **Findings:**

| Severity | File:Line | Issue | Resolution |
|----------|-----------|-------|------------|
| P1 | client.ts:155 | Child process not cleaned up when initialize fails (timeout/reject) | Fixed: try/catch around init handshake, dispose connection + kill process on failure |
| P2 | client.ts:154 | `clearTimeout` only runs on success path | Fixed: moved into catch block + after race |
| P2 | client.ts:298 | Shutdown timeout timer never cleared when server responds promptly | Fixed: store timer ref + `clearTimeout` after shutdown race |

### 6. Codex PR Review — Round 3
- **Findings:**

| Severity | File:Line | Issue | Resolution |
|----------|-----------|-------|------------|
| P2 | manager.ts:47 | Extension → language → find() misroutes when multiple configs share same language | Fixed: map extension directly to LspServerConfig + enforce unique languages in constructor |
| P2 | client.ts:270 | Pending restart not canceled on shutdown — can spawn after dispose | Fixed: check `disposed` after backoff, await `initPromise` in shutdown |

### 7. Codex PR Review — Round 4
- **Findings:**

| Severity | File:Line | Issue | Resolution |
|----------|-----------|-------|------------|
| P1 | manager.ts:60 | Manager-triggered reinit doesn't rehydrate open documents | Fixed: extracted `rehydrateDocuments()`, called from both `initialize()` and `ensureReady()` |
| P2 | client.ts:186 | `openClose` capability discarded when parsing TextDocumentSyncOptions | Fixed: track `openCloseSupported` separately, gate didOpen/didClose independently from didChange |

**All 22 findings (plan + implementation + 4 PR review rounds) fixed and verified.**
Tests: 58/58 pass. Typecheck: clean. Build: clean. Semgrep: 0 findings.
